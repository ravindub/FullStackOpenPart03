{
  "name": "phonebook",
  "version": "1.0.0",
  "description": "phonebook backend",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:ui": "@powershell Remove-Item -Recurse -Force dist && cd phonebookFrontEnd && npm run build && cd .. &&@powershell Copy-Item -Recurse phonebookFrontEnd/dist dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ravindu Bandara",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^7.5.3",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
